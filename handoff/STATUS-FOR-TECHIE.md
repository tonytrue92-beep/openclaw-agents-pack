# Status для технаря — что висит и что приоритетно

**Обновлено:** 2026-06-10

Этот документ — **главный entry-point** для технаря. Здесь:
- ✅ что закрыто
- ⏳ что ждёт работы (с приоритетами и оценками)
- 📁 ссылки на детальные брифы

Используй этот файл как dashboard. Когда закроешь задачу — напиши
Антону одной строкой, он обновит статус.

---

## 🔐 НОВЫЙ ПРИОРИТЕТ 2026-06-12 — защита IP (закрыть «codex тянет без токена»)

Клиент: «просто даёшь codex установщик — он всё пиздит без токена». Подтверждено:
шаблоны+база знаний качаются с ПУБЛИЧНЫХ raw-ссылок, токен в скачивании не
участвует; плейнтекст KB лежит и в истории git. Решение Антона: защищаем
поэтапно. Реальный фикс = **private-репо + доставка IP по токену** (бот меняет
команду — поэтому нужен ты). Полное ТЗ и порядок переключения без простоя:
📄 `handoff/IP-PROTECTION-techie-spec.md`. Связать с отзывом токенов (отозванный
токен теряет доступ к IP) и подумать про ротацию выжимок после флипа.

---

## ✅ Закрыто технарём 2026-06-10 (вечер) + одна правка

Технарь обновил прод-бота: одна команда для платного токена, minimax убран,
curator-guide синхронизирован, TRY-поток не тронут (верно). Бэкап и тесты есть.

### ⚠️ Единственная правка: требования платного потока
В боте сейчас «ChatGPT-аккаунт + BotFather». **ChatGPT-аккаунт НЕ нужен для
установки** — установщик просит:
1. **Бесплатный API-ключ opencode.ai** (sk-…, карта не нужна) — для дефолтной
   модели DeepSeek (`opencode-go/deepseek-v4-flash`);
2. **Токен бота из @BotFather** (на Pro — по токену на каждого выбранного агента);
3. **Telegram ID** клиента.
ChatGPT-аккаунт — **опционально, после установки**, для апгрейда мозгов
командой `openclaw-add-codex`. Иначе клиенты придут к шагу ключа неготовыми
(«где создать API key?» — уже ловили в саппорте 2026-06-10).

---

## 🚨 ОБНОВЛЕНИЕ 2026-06-10 (вечер) — по итогам дня живого саппорта

Прошёл первый день массовых установок. Главные выводы для бота:

### 1. Пункт A (одна команда) теперь КРИТИЧЕН — клиенты ставят по старым командам
Половина ошибок дня — люди запускали **старые команды из материалов недели 1**
(два шага, второй установщик без первого, `--collect-debug` вместо установки).
Пока бот/уроки шлют старое — саппорт будет тонуть. Просьба: заменить
в боте на одну команду (см. пункт A ниже) **в первую очередь**.

### 2. В текстах бота заменить упоминания модели
OpenClaw переименовал провайдера: `opencode` → `opencode-go`. Старая модель
`opencode/minimax-m2.5-free` **больше не существует** (клиенты ловили
«Unknown model»). Если в сообщениях бота/уроках упоминается «minimax» —
заменить на: **«бесплатная DeepSeek (`opencode-go/deepseek-v4-flash`)»**.
Установщики уже исправлены: factory `2026.06.10.1`, agents-pack
`v2026.06.10.3` (bundled в releases/latest), test-drive `2026.06.10`.

### 3. Куратору (если у бота есть саппорт-инструкция) — свежий curator-guide
`docs/curator-guide.md` пополнен сценариями дня (раздел 5.13a–d):
- `Unknown model: opencode/minimax…` → `openclaw-switch-model opencode-go/deepseek-v4-flash`
- agents-pack падает на «умной памяти» (R1.5) → `--install --no-embedding`
  (с `2026.06.10.3` дефолт уже «без памяти», ошибки не валят установку)
- «Disable N unavailable skills? → No → Setup cancelled» → `openclaw doctor --fix --yes`
  (новый установщик делает это сам)
- WSL: systemd для gateway, сброс сети после `wsl --install`, init→VPS
Выдача токенов — без изменений. TRY-блокер (1️⃣) и отзыв старых токенов (D) в силе.

---

## 🔥 СВЕЖИЙ ПРИОРИТЕТ (2026-06-06) — почти всё на стороне `@AITeamVIPBot`

В проде накатили: объединённый установщик (одна команда), Windows-интерфейс,
аналитика установок (Worker готов, деплой за Антоном). По важности:

### ✅ A. Одна команда установки (платный поток) — СДЕЛАНО 2026-06-10

Платный установщик объединён: factory сам дотягивает агентов по тарифу из
токена. Бот должен слать **одну** команду (с подставленным токеном), а не две:
```
bash <(curl -fsSL https://raw.githubusercontent.com/tonytrue92-beep/openclaw-factory/main/scripts/demo-install.sh) --course-token <ТОКЕН>
```
VIP/STD → агенты сами; SUB → только движок. Убрать «запусти вторую команду».
📄 Бриф: `handoff/unified-install-command-bot-brief.md`.

### 🆕 B. Аналитика установок — эндпоинты хостишь ТЫ + бот шлёт `/issue`

Cloudflare НЕ используем (решение Антона, 2026-06-10). Эндпоинты
(`/issue`/`/activation`/`/stats`) поднимаешь **на своём сервере** (рядом с
Prodamus-webhook). Готовая логика и схема: `handoff/analytics-endpoint-reference/`.
Бот при выдаче токена шлёт `POST /issue` с
`{token_hash=sha256(token), tg_id, email, tier}` (заголовок `X-Admin-Key`,
секрет — твой). Когда поднимешь — URL Антону (включит пинги установщиков).
📄 Бриф: `handoff/install-analytics-bot-brief.md`.

### ℹ️ C. Windows-интерфейс (Companion) — FYI, в боте опц.

Установщик сам в финале предлагает Windows-клиенту OpenClaw Windows Hub. В боте
правок не требуется; опц. — в Windows-инструкции добавить «в конце предложит
интерфейс, соглашайся».

### 🔴 D. Отозвать токены прошлого потока (НЕ трогая ручные)

Деактивировать в БД бота токены/доступы прошлого потока (бот их больше не
признаёт/не выдаёт команду). **Ручные токены не трогать.** ОБЯЗАТЕЛЬНО:
dry-run → согласовать список с Антоном (ручные исключены) → применить.
Нюанс: установщик проверяет подпись локально (серверной revocation нет) —
деактивация отрезает выдачу через бота, но не аннулирует уже сохранённый токен;
жёсткий отзыв = ротация ключа (отдельно).

### ℹ️ E. Pro: выбор сколько/каких агентов — FYI, команда НЕ меняется

С `agents-pack 2026.06.09` на Pro установщик сам спрашивает, **сколько и каких**
агентов ставить (список с номерами, Enter = все 8). **Команда установки та же** —
обновлять в боте нечего. Опц. — в продающем тексте про Pro можно упомянуть «сам
выберешь, кого из 8 агентов поставить». Бонус: меньше агентов = меньше бот-токенов.

> 📄 Все задачи бота одним файлом для пересылки: `handoff/TECHIE-TASKS-2026-06-09.md`

### 1️⃣ БЛОКЕР — TRY-токен после оплаты (тест-драйв)

Новый продукт **тест-драйв** (репо `openclaw-test-drive`) ставится ТОЛЬКО
по **TRY-токену**. Сейчас бот их не выдаёт → продукт нельзя поставить вообще.

- Поток (вариант B): оплата на сайте (**Prodamus**) → webhook к нам →
  бот по одноразовому deep-link выдаёт TRY-токен и шлёт команду установки.
- Тот же Ed25519-ключ, новый payload `TRY|<hash16>|<tg_id>`.
- 📄 Полный бриф с кодом подписи: **в репо `openclaw-test-drive`** →
  `handoff/trial-token-brief-for-techie.md`.
- Команда, которую шлёт бот после оплаты:
  `bash <(curl -fsSL .../openclaw-test-drive/main/scripts/install-trial.sh) --token TRY-XXXX`

### 2️⃣ Бот-копия: Pro теперь **8 агентов** (было 6)

В Pro добавились **💰 Лидоруб** (продажи/закрытие заявок) и
**🎥 Контент-агент** (видео + озвучка). Везде в боте, где «6 агентов» или
перечень Pro → **8 агентов** + добавить эти два в описание «что входит».

### 3️⃣ Install-команды в боте — актуализировать

- Платный «мозги»-шаг сейчас = **API-ключ opencode.ai** (бесплатная модель,
  карта не нужна). Codex (вход в ChatGPT) — опц. апгрейд: в обязательный
  поток НЕ вшит, но хелпер `openclaw-add-codex` ставится установщиком
  автоматически — клиенту достаточно одной команды (см. пункт 4️⃣).
  Сообщения про opencode-ключ — оставить.
- Ссылки — latest-tracking (`releases/latest` / `/main/`), версии не хардкодить.
- Добавить совет про ботов: **создавать партиями по 2-3 с паузой** (на Pro
  нужно 8 ботов; быстрое создание подряд → Telegram блок на ~сутки).
- 📄 Бриф: `handoff/bot-install-commands-update-brief.md` (в этом репо).

### 4️⃣ ✅ Codex/ChatGPT-мозги — РЕШЕНО (хелпер `openclaw-add-codex`)

Разобрались (клиент, OpenClaw 2026.6.1): `openai-codex` — **legacy provider id**.
На 2026.6.x вход в ChatGPT — через provider **`openai`**, плюс нужен Codex-плагин.
В factory (`2026.06.06.2`) добавлен opt-in хелпер **`openclaw-add-codex`**: ставит
плагин (`clawhub:@openclaw/codex`→npm) → enable+refresh → restart → вход
`--provider openai` → модель `openai/gpt-5.5`. Клиенту — одна команда.
**На стороне бота ничего не нужно** (это локальный хелпер). 📄 `handoff/codex-login-for-techie.md`.

> Старые задачи ниже (wave 20 rebrand в боте, SUB-tier, HRM) — остаются,
> но приоритет 1-3 выше. Если делаешь rebrand — заодно закрой пункт 2.

---

## ✅ Закрыто (на твоей стороне)

| Wave | Что | Где | Когда |
|---|---|---|---|
| 17 | Санитизация ввода токена в factory | factory PR #2 (закрыт PR #2 содержит и 17 и 17b) | май 2026 |
| 17b | `--course-token` флаг + ENV в factory | factory PR #2 | май 2026 |
| 18 | UX-чистка factory `demo-install.sh` | factory PR #5 (−720 строк) | 2026-05-24 |

---

## ⏳ Что висит — в порядке приоритета

### 🥇 Приоритет 1 — Wave 20 rebrand (factory + бот)

**Что**: переименовать тарифы в UI-выводе и сообщениях клиентам.

| Внутри (token-payload) | Публично |
|---|---|
| `SUB` | **OpenClaw** |
| `STD` | **Base** |
| `VIP` | **Pro** |

**Где менять**:
1. `openclaw-factory/scripts/demo-install.sh` — UI-выводы (echo/explain
   блоки) — **30 мин**
2. `@AITeamVIPBot` — сообщения при выдаче токена, истёкшая подписка,
   `/help`, описание тарифов — **1-2 часа**

**ВАЖНО**: внутренние `COURSE_TIER` (`STD`/`VIP`/`SUB`), payload
токена, Ed25519-ключи — **не меняем**. Backwards-compat 100%.
Старые токены работают.

**Бриф**:
- 📄 Полный TZ: [`wave-20-rebrand-brief.md`](./wave-20-rebrand-brief.md)
- 🤖 Промпт для AI-coder (только factory): [`wave-20-factory-rebrand-AI-coder-prompt.md`](./wave-20-factory-rebrand-AI-coder-prompt.md)
- Reference: agents-pack PR #30 (https://github.com/tonytrue92-beep/openclaw-agents-pack/pull/30/files)

**Estimate**: 1.5-2.5 часа (factory + бот вместе)

**Почему срочно**: сейчас бот выдаёт «Твой VIP-токен», а установщик
показывает «Pro» — клиент путается «мне дали VIP, а тут Pro, что
это?». Лучше синхронизировать.

---

### 🥈 Приоритет 2 — Token-first flow в factory

**Что**: перенести валидацию курс-токена ВЫШЕ меню в `demo-install.sh`.
Сейчас опции «Демо» / «Симуляция» / «VPS-гайд» проходят **без токена**
— фактически дают инструкцию как поставить руками без покупки.

**Бриф**: [`factory-token-first-brief-for-techie.md`](./factory-token-first-brief-for-techie.md)

**Reference**: наш wave 14 в agents-pack (точный паттерн — `step_header "V0"`
до меню):
https://github.com/tonytrue92-beep/openclaw-agents-pack/blob/main/scripts/install-agents.sh

**Estimate**: 45 минут (просто перенос блока + smoke-test + bump version)

---

### 🥉 Приоритет 3 — SUB-tier в `@AITeamVIPBot` v4

**Что**: добавить третий tier (subscription / OpenClaw) в `@AITeamVIPBot`:
- Новая таблица `subscribers` (SQL-схема в брифе)
- Команда `/admin_upload_subscribers` для импорта CSV
- Логика поиска при `/start`: VIP → STD → SUB (3-level)
- Проверка истечения подписки → сообщение «подписка истекла»
- v4 payload `SUB|hash|tg` (тот же Ed25519-ключ)

**Бриф**: [`subscription-tier-brief-for-techie.md`](./subscription-tier-brief-for-techie.md)

**CSV для импорта** (41 текущий подписчик):
[`active_subscribers-2026-05-11.csv`](./active_subscribers-2026-05-11.csv)

**Estimate**: 5-6 часов

**Зависимости**: до этого нужно сделать **Wave 20 rebrand** на стороне
бота (иначе SUB-сообщения сразу пишутся под старым неймингом, потом
придётся переписывать).

---

## 🆕 Что сделано на нашей стороне за последнюю неделю

Мы (`openclaw-agents-pack`) **дальше** ушли в проде, чтобы клиентам
было лучше. Тебе **не нужно** что-то с этим делать в коде — просто
для контекста, чтобы понимать что видит клиент.

| Версия | Wave | Что добавилось |
|---|---|---|
| `v2026.05.15` | 17 | Санитизация токена при копи-паста |
| `v2026.05.17` | 18 | UX-чистка (−56 строк объяснений) |
| `v2026.05.17.1` | 19 | Авто-detect платформы в финале |
| `v2026.05.24` | 20 | Banner + меню → Base/Pro/OpenClaw |
| `v2026.05.24.1` | **21** | **Главное меню V_MAIN после banner — до токена** |

### Wave 21 — главное меню (только что задеплоено)

Сразу после ASCII-баннера показывается:

```
   ╔════════════════════════════════════════════════════════╗
   ║                  Г Л А В Н О Е   М Е Н Ю               ║
   ╚════════════════════════════════════════════════════════╝

   Что ставим?

     1)  Pro        — 8 агентов (полный набор)  ← рекомендуется
     2)  Base       — 3 базовых агента
     3)  OpenClaw   — только движок (без агентов)
```

Это «витрина → корзина → оплата» — клиент видит линейку продуктов
сразу, потом подтверждает токеном. UX как у магазина.

**Если выбран OpenClaw** → graceful exit без запроса токена (движок
уже стоит из factory).

**Если выбран Pro/Base** → запрос токена → валидация tier vs выбор:
- Pro+VIP-токен → OK, 8 агентов
- Pro+STD-токен → red-block «нужен Pro-токен»
- Base+STD/VIP → OK, 3 агента (Pro→Base downgrade разрешён)
- Base+SUB → red-block

V_MAIN **пропускается** если есть флаги `--install` / `--course-token`
/ `--only-agent` / `--config` / `--vps` (для CI и продвинутых
сценариев).

CHANGELOG: https://github.com/tonytrue92-beep/openclaw-agents-pack/blob/main/CHANGELOG.md#2026-05-24--wave-21

---

### 💡 Опционально — главное меню в factory тоже?

Сейчас в `demo-install.sh` есть собственное меню «Демо / Реальная /
Симуляция / VPS-гайд» (4 опции). Если хочешь — можно тоже сделать
**главное меню** в начале с понятными названиями:

```
1) Установить OpenClaw  ← реальная установка
2) Демо-режим           (10 шагов с объяснениями, без установки)
3) VPS-гайд             (инструкция для сервера)
```

И **убрать** «Симуляция (dry-run)» в скрытый флаг `--dry-run` — это
эксперт-режим.

**НЕ срочно**. Делается после wave 20 rebrand + token-first flow.

Если приоритет важнее — скажи, я отдельный бриф напишу.

---

## 📋 Общие правила работы

1. **Версионирование**: каждая правка → bump `INSTALLER_VERSION` →
   commit → PR → review → merge. Я обычно мержу за Антона если CI
   зелёный — но для крупных правок (типа SUB-tier) лучше Антон сам.

2. **Smoke-test обязательно**: `bash -n` + локальный smoke-test
   перед push. CI отловит большинство багов, но локальный быстрее.

3. **SHA256SUMS**: если правишь скрипты — `bash scripts/update-checksums.sh`
   перед коммитом. Иначе CI job «SHA256SUMS freshness» зафейлится.

4. **Backwards-compat**: токены / payload / Ed25519-ключи / внутренние
   `tier`-имена (STD/VIP/SUB) — **никогда не меняем**. Меняем только
   что **клиент видит**.

5. **Когда закрыл задачу** — пиши Антону одной строкой:
   ```
   Wave NN готов, версия X.Y.Z, PR #N в main
   ```
   Антон обновит этот status-документ.

---

## 🆘 Если застрял

- В каждом брифе есть **AI-coder промпт** — копируй в Cursor/Claude
  Code, агент закроет 5-15 минут вместо часа вручную.
- Reference-код всегда в `openclaw-agents-pack` (наш репо) — там же
  pattern для всего что мы просим в factory/боте.
- Если вопрос не очевиден — пиши Антону, он переспросит у меня.

---

## История обновлений

- **2026-05-25**: создан документ. Закрыто: wave 17/17b/18. Висит:
  wave 20 (rebrand), token-first, SUB-tier.
