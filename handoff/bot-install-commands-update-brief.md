# Бриф технарю — обновить install-команды в @AITeamVIPBot

## Задача

Обновить команды установки, которые бот присылает клиенту **после выдачи
токена**, чтобы они: (1) указывали на актуальные репозитории, (2) всегда
тянули свежую версию, (3) уже содержали выданный токен (клиент не вставляет
его вручную).

## Золотое правило: НЕ вшивать номер версии в URL

Используем ссылки, которые **сами отдают последнюю версию** — тогда бот
никогда не придётся править при каждом релизе:

| Что | URL (всегда свежак) | Почему такой |
|----|--------------------|-------------|
| Движок (1 ступень, платный) | `https://raw.githubusercontent.com/tonytrue92-beep/openclaw-factory/main/scripts/demo-install.sh` | `/main/` = последний коммит; скрипт самодостаточный |
| Агенты (2 ступень, платный) | `https://github.com/tonytrue92-beep/openclaw-agents-pack/releases/latest/download/install-agents-bundled.sh` | `releases/latest` = последний релиз; **bundled** (самодостаточный) |
| Тест-драйв (TRY) | `https://raw.githubusercontent.com/tonytrue92-beep/openclaw-test-drive/main/scripts/install-trial.sh` | `/main/` = последний коммит; скрипт самодостаточный |

⚠️ **Важно про agents-pack:** для него использовать ТОЛЬКО `releases/latest/.../install-agents-bundled.sh`.
Не `raw .../main/scripts/install-agents.sh` — тот не самодостаточный
(подключает `lib/*.sh`, которых нет при запуске через `curl`). У factory и
trial наоборот — берём `raw /main/` (они одним файлом).

## Токен подставляем сразу (бот его знает)

Не «вставь токен когда попросит», а сразу в команду:

- **Тест-драйв (TRY-токен):** флаг `--token <ТОКЕН>`
- **Платные (SUB / STD / VIP / HRM):** флаг `--course-token <ТОКЕН>` (на обеих ступенях)

## Готовые сообщения бота (подставить реальный токен вместо <ТОКЕН>)

### A. После оплаты тест-драйва (TRY-токен)

```
Готово ✅ Доступ к тест-драйву AI TEAM 2.0.

Открой Терминал (на Mac: Cmd+Пробел → «Терминал») и вставь:

bash <(curl -fsSL https://raw.githubusercontent.com/tonytrue92-beep/openclaw-test-drive/main/scripts/install-trial.sh) --token <ТОКЕН>

Дальше установщик сам всё поставит. Понадобится: токен бота из @BotFather.
```

### B. Платный тариф (SUB / STD / VIP / HRM) — 2 шага

```
Готово ✅ Твой доступ к AI TEAM 2.0. Ставим в 2 шага.

ШАГ 1 — движок (вставь в Терминал, дождись «готово»):

bash <(curl -fsSL https://raw.githubusercontent.com/tonytrue92-beep/openclaw-factory/main/scripts/demo-install.sh) --course-token <ТОКЕН>

ШАГ 2 — твои AI-агенты (когда первый закончит):

bash <(curl -fsSL https://github.com/tonytrue92-beep/openclaw-agents-pack/releases/latest/download/install-agents-bundled.sh) --course-token <ТОКЕН>

По ходу понадобится: API-ключ модели (из материалов курса) и токен бота из @BotFather.
```

## Чек-лист

- [ ] Заменить ВСЕ старые команды, что бот шлёт сейчас (старые URL до ребренда,
      raw-ссылку agents-pack для trial, команды без токена) на блоки A/B выше.
- [ ] Токен подставляется автоматически (бот его только что выдал, он привязан
      к TG ID клиента — анти-шаринг сохраняется).
- [ ] Версию в URL НЕ хардкодить (см. золотое правило) — ссылки сами свежие.
- [ ] Секреты клиента (токен @BotFather, API-ключ модели) НЕ подставляем —
      их клиент вводит сам в процессе установки. У бота их нет и быть не должно.
- [ ] macOS/Linux — команда работает как есть. Windows — нужен WSL/Git Bash
      (установщик сам определит ОС и подскажет).

## Контекст версий (на момент брифа)

- Движок (factory): актуальная в `/main/`.
- Агенты (agents-pack): релиз **v2026.06.02** (важный фикс: embedding-память
  теперь реально включается; в прошлых версиях были ошибки «Config validation
  failed»). `releases/latest` уже указывает на него.
- Тест-драйв (test-drive): актуальная в `/main/` (вход по TRY-токену после оплаты).
